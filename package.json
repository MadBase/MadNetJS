{
  "name": "madnetjs",
  "version": "1.0.0-rc.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive tests/**/*.js --timeout 45000",
    "test-unit": "mocha --recursive tests/unit --timeout 45000",
    "test-integration": "mocha --recursive tests/integration --timeout 45000",
    "test-single": "mocha --timeout 45000",
    "test-coverage": "npx nyc@15.1.0 --exclude=src/GoWrappers/** npm run test",
    "test-unit-coverage": "npx nyc@15.1.0 --exclude=src/GoWrappers/** npm run test-unit",
    "test-integration-coverage": "npx nyc@15.1.0 --exclude=src/GoWrappers/** npm run test-integration",
    "build-docs": "npx jsdoc -R README.md src/ -r -d docs/ -c jsdoc.json",
    "lint": "eslint .",
    "lint-fix": "eslint --fix ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "0.20.0",
    "ethereumjs-util": "7.0.4"
  },
  "devDependencies": {
    "assert": "2.0.0",
    "better-docs": "2.3.2",
    "chai": "4.2.0",
    "chai-as-promised": "7.1.1",
    "dotenv": "8.2.0",
    "eslint": "8.8.0",
    "jsdoc": "3.6.6",
    "mocha": "8.2.1"
  }
}
