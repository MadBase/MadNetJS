/**
 * @typedef {Object} WalletParams
 * @property {Array<hex>} chainId - ChainID of the network to be connected to
 * @property {Array<hex>} rpcServer - RPC Endpoint to use for RPC requests 
 * @property {String} rpcTimeout - Maximum time to wait for RPC requests
 */